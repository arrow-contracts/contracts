# Foundry build artifacts — regenerated by `forge build`
out/
cache/

# Third-party dependencies — reinstalled via `forge install`, see README
lib/

# Foundry's local dev node state
broadcast/**/dry-run/
